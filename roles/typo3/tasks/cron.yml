---

- name: setup cron scheduler tasks for TYPO3 CMS
  cron: name="Scheduler task for {{ item.key }}" job="php {{ typo3_webroot }}{{ item.key }}/typo3/cli_dispatch.phpsh scheduler"
  with_dict: typo3.cms.sites
  tags: [typo3-cms-cron]

